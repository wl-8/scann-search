### ⚠️ API 文件写了但没有 UI 页面

| 接口 | 状态 |
| :--- | :--- |
| `POST /auth/register` | `auth.ts` 有函数，`LoginView` 没有注册表单 |
| `GET /users` 等全套用户管理 | `users.ts` 完整，但 `UserManageView` 是空 TODO |

---

### ❌ 后端有但前端完全没有对接

| 接口 | 说明 |
| :--- | :--- |
| `POST /search/compare-strategies` | 多过滤策略对比，无 UI |
| `POST /search/batch` | api 层写了但 `MultiSearchView` 是占位符 |
| `POST /benchmark/run` 等全套 | `BenchmarkView` 是空 TODO |
| `GET /export/*` 三个 | `ExportView` 是空 TODO |
| `PUT /datasets/{id}/embedding` | 切换 embedding key，无 UI |
| `GET /datasets/{id}/cells` | 分页浏览细胞，无 UI |
| `POST /datasets/{id}/cells/filter` | 条件过滤细胞，无 UI |
| `POST /visualize/{id}/locate` | 高亮定位，无 UI |